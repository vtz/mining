# Railway configuration for NSR Backend
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Use Dockerfile CMD (entrypoint.sh)
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

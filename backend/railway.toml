# Railway configuration for NSR Backend
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# No healthcheck - let container run
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
